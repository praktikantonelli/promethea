# Server Application

This is the main backend application for the web server. It uses `axum` as the REST backend handler crate. The backend is accessed from the frontend via specific API routes. 
